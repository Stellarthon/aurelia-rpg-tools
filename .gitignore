# Claude Code local, machine-specific settings (permission grants etc.)
.claude/settings.local.json

# Private rules reference extracted from the referee's own rulebooks — never publish
.claude/skills/traveller-2e-rules/

# Self-contained local-viewing bundle (built by tools/build-local.mjs; the modular index.html is the source of truth)
index.local.html

# Agnostic SciFiVTT distribution — generated by tools/build-agnostic.mjs, published to the separate public repo
dist-scifivtt/

# Transient dev dependency (playwright-core) used only for local boot verification
node_modules/
package-lock.json
